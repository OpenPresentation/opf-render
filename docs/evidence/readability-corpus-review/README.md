# Readability floor raster review

Core `ade2a4f0a8afa93f546a3b1e18f2ed23b84d0ac7` raises actual text fragments to the selected minimum and bounds fitting searches. The renderer product remains `c7a7387f6e27cc21eeeec3015492afe289ec4ce3`. This checkpoint accepts the resulting corpus changes, without changing renderer product code or font bytes.

The source remains 126 decks / 805 slides with an identical source digest. 262 slides change and 543 remain byte identical against the accepted source-whitespace checkpoint. All 33 paired sheets and all six full-size before/after pairs were visually inspected. The changes increase table text and shared chart category labels; wrapping remains within the reviewed cells. No new clipping was observed. Existing sparse layouts, unresolved assets/data, small template footers/page numbers, missing nested bullet glyphs and low-contrast text remain separate work. This is a regression review, not general visual-quality acceptance or native Office evidence.

`test/golden/opf-examples-png.readability.sha256.json` is a separate accepted baseline. Historical baselines and the initial failing gate are retained. `report.json` binds the exact predecessor, candidate, source digest, review script, sheets and full-slide files. The unchanged predecessor runtime and retained core tarball reproduce all 805 accepted source-whitespace PNG hashes; those hashes were independently checked before this review.

To reproduce, extract renderer `c7a7387f6e27cc21eeeec3015492afe289ec4ce3` using `git archive` for `dist` and `test/golden.mjs` into `artifacts/readability-before-runtime`. Safely extract `predecessor-core-snapshot.tgz` with its `package/` prefix removed into that directory's `node_modules/@openpresentation/opf`; other dependencies resolve from this checkout. `predecessor.json` records the original candidate manifest and SHA-256 of every extracted file. Use the `previousCoreCandidate` object in `report.json` as that runtime's `before.json`. Generate predecessor and candidate PNGs into `artifacts/source-whitespace-golden-after` and `artifacts/readability-golden-after` using `--update` with separate `OPF_GOLDEN_OUT` paths, then run `node test/readability-review-sheets.mjs`. Candidate generation does not promote a baseline.

The separate core browser harness checks 48 measured/estimated wide/portrait cases at floors 16/24/32 on Node 20 and 24. It verifies actual SVG computed font sizes and independently parsed editable PPTX run sizes. Four full fixture screenshots were inspected, including the intentionally amplified rich-size ratios. That check does not certify font identity, native ink equivalence or all template text. Versions, release plans and deployments are unchanged.

`manifest.json` hashes every retained evidence file except itself.
