# Shared timeline raster review

This candidate restores authored timeline names/descriptions, preserves each event field and uses shared layout accepted by core. The source remains 126 decks / 805 slides with the same source digest. Against the accepted readability checkpoint, 83 slides change and 722 remain byte identical. All 11 paired sheets and all six full-size before/after pairs were visually inspected. No new clipping was observed in those views. Existing sparse layouts, low-contrast connectors, small footers and missing asset/data or marker coverage remain separate work; this is a scoped regression review, not overall presentation-quality or native Office acceptance.

`source-graph.json` records the four product commits; `report.json` records each review image and its hash. `predecessor-core-snapshot.tgz` is the exact unpublished readability core candidate, SHA-256 `b47b80fa6c4279b565a76643a415a79ababbdf14e5caa639c6142b625b532859`. `predecessor-manifest.json` records the original candidate packages. The independently archived predecessor runtime reproduced all 805 accepted hashes (`predecessor-verify.log`).

To reproduce, archive renderer `53b90878b8a9610117ac5191ebfaaa549cc2f3ea` (`dist` and `test/golden.mjs`) into `artifacts/timeline-before-runtime`. Safely extract the verified core archive, removing its `package/` prefix, into that directory's `node_modules/@openpresentation/opf`; remaining dependencies resolve from this checkout. Copy `predecessor-manifest.json` to that runtime's `before.json`. Generate predecessor and candidate PNGs with `--update` into separate `artifacts/readability-golden-after` and `artifacts/timeline-golden-after` directories, then run `node test/timeline-review-sheets.mjs`. Generation only creates a candidate; review precedes promotion.

The accepted candidate is retained separately at `test/golden/opf-examples-png.timeline.sha256.json`. All prior baselines remain available. No source package version, registry release or deployment changed. `SHA256SUMS.json` covers the portable evidence files, excluding itself.
