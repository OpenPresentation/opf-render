# Renderer 0.7.0 release gates

Branch `codex/shared-code-release-20260909` prepares renderer 0.7.0 from the tested code integration `b5a3722725338e8d964783e8b7f1f7b80ce28cd3`. Core 0.9.0 and CLI 0.7.0 are published from reviewed core merge `685733976a665d0190bdb9922b9a174ef94153d9`. Renderer 0.7.0 is still a candidate. Its lockfile now resolves actual npm core 0.9.0; unrelated dependencies are unchanged.

Complete local checks pass on Node 20.20.2 and 24.20.0: syntax and package validation, all package suites, 126 decks / 805 reviewed raster hashes, code source/metadata preservation and 132 XML-character rejection cases, clean packed installs, JPEG/quote browser suites and ten wide/portrait code browser cases. Edge 152.0.4191.66 loads the actual bundled Cousine regular/bold bytes. Source offsets and accepted segment starts remain within the original 0.1 reference-pixel gate; glyph containment, metadata separation and absence of runtime network requests pass.

Portable [Node 20 evidence](evidence/shared-code-release/node20.json) and [Node 24 evidence](evidence/shared-code-release/node24.json) bind the 16 shipped file hashes, actual registry core integrity, detailed browser results and full-check log hashes. The final local renderer archives have identical integrity on both runtimes: `sha512-iDAAjXEZKlYuNvxbuJurf5wr2Uehb9hbSBiuy7FEylLcIqBUcw5lfA8j0+/e+D2uCe0YiEVoaNdxWvFnmNd4zg==`. The candidate has zero known audited advisories. These local archive bytes are not a registry publication claim.

Both CI and trusted publication now require the code Node and code-browser suites in addition to the existing gates. The packed installer also executes code assertions through the actual installed public entrypoints. Full Linux CI and automated review must pass on the final PR head before merge. Verify the merge tree matches that reviewed head, then publish a new `opf-render-v0.7.0` tag only if neither tag nor npm version exists.

After publication, verify the actual npm gitHead, integrity, provenance/signature, all shipped files against the immutable merge, pinned source fixtures, raster corpus and actual loaded-font browser workflows on Node 20/24. Core's `scripts/test-renderer-publication.mjs` adds code regressions and ten code browser cases for renderer 0.7+. Only then refresh PPTX 0.7.0 and editor 0.6.0 dependency locks. Keep the complete-set release plan and public deployments on the last fully verified set until every successor passes its own registry gates.

The earlier 43 changed code rasters have a separate [review](evidence/shared-code/raster-review.json); the other 762 hashes are unchanged. Regression stability, schema validity, source/metadata preservation, browser glyph placement and native PowerPoint fidelity are distinct. No native raster equivalence, unrestricted XML serialization, universal glyph coverage or complete ecosystem adoption is claimed here.
